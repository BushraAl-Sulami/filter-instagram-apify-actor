{
  "title": "Filter Instagram Restaurants",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "datasetId": {
      "title": "Source Dataset",
      "type": "string",
      "description": "Choose the Apify Dataset containing the restaurant records to filter.",
      "editor": "resourcePicker",
      "resourceType": "dataset",
      "resourcePermissions": ["READ"]
    },
    "instagramField": {
      "title": "Instagram URL field",
      "type": "string",
      "description": "Name of the field containing the Instagram URL.",
      "editor": "textfield",
      "default": "instagramUrl"
    }
  },
  "required": [
    "datasetId"
  ]
}